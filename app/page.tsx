import Orchestrator from "@/components/orchestrator/Orchestrator";

// The whole site is one conversational surface. Sections are responses, not pages.
export default function Home() {
  return <Orchestrator />;
}
