---
# TODO_ABELITO: DEMO content — coherent placeholder to showcase the chat, NOT real
# claims about your work. Replace with the real Manna story in M4.
slug: manna
title: Manna
tagline: A personal-finance app that turns messy bank exports into a weekly spending picture.
agent: builder
blocks:
  - type: overview
    body: >
      Manna is a personal-finance app that turns messy bank exports into a clear
      weekly spending picture. This case study covers the two pieces that make or
      break that experience: the transaction import pipeline and the
      categorization engine.
  - type: role
    body: >
      I built the import and categorization end to end — the statement parser,
      the rules-plus-model classifier, and the sync layer that keeps categories
      consistent as new transactions arrive.
  - type: approach
    body: >
      Every transaction runs through a fast rules pass first; anything ambiguous
      falls through to a small classifier for the long tail. Users correct a
      category inline, and that correction becomes a new rule — so the system
      needs the model less over time, not more.
  - type: architecture
    image: "TODO_ABELITO: architecture diagram"
  - type: stack
    tags: [React, Node.js, AWS, Redis]
  - type: retrospective
    quote: >
      Most “AI categorization” problems are really rules problems with a model
      for the long tail — the craft is knowing which transactions are which.
---
