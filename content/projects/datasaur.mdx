---
# TODO_ABELITO: DEMO content — coherent placeholder to showcase the chat, NOT real
# claims about your work. Replace with the real Datasaur story in M4.
slug: datasaur
title: Datasaur
tagline: An LLM-assisted auto-labeling assistant for NLP annotation teams.
agent: builder
blocks:
  - type: overview
    body: >
      Datasaur is an NLP data-labeling platform where teams annotate text to
      train language models. This case study covers the auto-labeling assistant:
      a feature that pre-annotates each document so human reviewers correct
      suggestions instead of labeling from scratch. It shipped as the default
      workflow for named-entity and classification projects.
  - type: role
    body: >
      I led the machine-learning engineering for the assistant — choosing the
      pre-labeling model, building the confidence router that decides what a
      human sees, wiring the reviewer feedback loop, and rolling it out to the
      first enterprise customers.
  - type: problem
    body: >
      Manual annotation was the bottleneck. Labeling a mid-size entity dataset
      took teams several weeks, and inter-annotator agreement drifted as fatigue
      set in — which slowed every downstream model that depended on the data.
  - type: approach
    body: >
      An LLM pre-labels every document, then a confidence router sends only the
      uncertain spans to human reviewers. Each correction is stored as a
      few-shot example, so the assistant gets sharper on a given project without
      any retraining.
  - type: architecture
    image: "TODO_ABELITO: architecture diagram"
    caption: "Pre-labeling service, confidence router, and the reviewer feedback loop."
  - type: results
    metrics:
      - label: Labeling throughput
        value: "3.2×"
      - label: Annotation accuracy
        value: "94%"
        delta: "+11 pts"
      - label: Reviewer time saved
        value: "60%"
  - type: stack
    tags: [TypeScript, Python, PyTorch, spaCy, Next.js, PostgreSQL]
  - type: timeline
    entries:
      - date: "2024 Q1"
        text: "Prototyped pre-labeling on a named-entity dataset."
      - date: "2024 Q3"
        text: "Shipped the confidence router and reviewer feedback loop."
      - date: "2025 Q1"
        text: "Rolled the assistant out to the first enterprise teams."
  - type: retrospective
    quote: >
      The win wasn’t the model — it was designing the human-in-the-loop so
      corrections were cheap to make and compounded on every later document.
---
